<%= form_for @listing, :url => listings_path, :html => { :multipart => true } do |f| %>
	<%= f.label(:name) %>
	<%=f.text_field :name %>
	<%= f.label :price %>
	<%= f.number_field :price %>
  	<%= f.file_field :avatar %>
	<%=f.submit %>

	<% end %>

